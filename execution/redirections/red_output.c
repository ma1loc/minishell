#include "mini_shell.h"

void    red_output(t_tree *tree, t_setup *setup)
{
	(void)tree;
	(void)setup;
}
// 	// echo "hello" > text.txt
// 	int		out_file;
// 	int		dup_stdout;

// 	out_file = open(tree->cmd->name, O_CREAT | O_WRONLY | O_TRUNC, "0644");
// 	if (out_file == -1)
// 		ft_perror(setup, NULL, EXIT_FAILURE);
// 	if (tree->cmd->name == )
	
// }